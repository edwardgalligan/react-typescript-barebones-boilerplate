# Absolute barebones minimal React + Typescript boilerplate

- What it says on the tin.
- Made for vscode but not necessarily.
- Requires bash in %PATH% if on Windows

## Usage

`npm install ; npm run build`
